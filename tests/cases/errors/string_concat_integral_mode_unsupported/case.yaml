id: errors.string_concat_integral_mode_unsupported
tags: [errors]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "integral-mode concatenation"
