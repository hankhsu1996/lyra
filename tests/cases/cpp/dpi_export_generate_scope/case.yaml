id: cpp.dpi_export_generate_scope
tags: [cpp_run]
input:
  command: [run, cpp]
  project: false
  top: Top
  files: [main.sv]
  link_sources: [dpi.c]
expect:
  exit: 0
  stdout:
    exact: |
      g0=100
      g1=101
