id: errors.event_dynamic_index_unsupported
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "unsupported:"
      - "compound event expressions"
    not_contains:
      - "internal error"
