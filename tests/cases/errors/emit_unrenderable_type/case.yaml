id: errors.emit_unrenderable_type
tags: [errors, emit]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
expect:
  exit: 1
  stderr:
    contains:
      - "unsupported:"
      - "packed array form is not yet supported in cpp emit"
    not_contains:
      - "internal error"
