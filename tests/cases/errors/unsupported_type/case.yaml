id: errors.unsupported_type
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "main.sv:2:"
      - "unsupported:"
      - "unpacked struct types are not supported"
      - "struct { int a; int b; } s;"
    not_contains:
      - "internal error"
      - "\x1b["
