id: errors.unsupported_expr
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "main.sv:4:"
      - "unsupported:"
      - "integral-mode concatenation is not yet supported"
    not_contains:
      - "internal error"
