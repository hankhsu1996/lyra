[benchmark]
name = "fanout-comb"
family = "scheduling"
focus = "runtime"
primary = "wakeup fanout / subscriber traversal / propagation pressure"
secondary = ["combinational evaluation overhead"]
regression_means = "scheduling/wakeup-fanout/subscriber-traversal regressed"

[tiers]
pr = true
nightly = true

[params.defaults]
NUM_CYCLES = 500000

[params.pr]
NUM_CYCLES = 50000

[params.nightly]
NUM_CYCLES = 500000

[counters]
relevant = []
