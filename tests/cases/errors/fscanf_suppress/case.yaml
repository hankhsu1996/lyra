id: errors.fscanf_suppress
tags: [cpp_run]
input:
  command: [run, cpp]
  project: false
  top: Top
  files: [main.sv]
expect:
  exit: 1
  stderr:
    contains:
      - "field width and assignment suppression are not yet supported"
