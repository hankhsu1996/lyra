id: errors.event_list_unsupported
tags: [errors, diag]
input:
  command: [dump, hir]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "main.sv:5:"
      - "unsupported:"
      - "event lists"
    not_contains:
      - "internal error"
