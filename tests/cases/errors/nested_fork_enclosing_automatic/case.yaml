id: errors.nested_fork_enclosing_automatic
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Test
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "unsupported:"
      - "fork-join branch"
      - "automatic"
    not_contains:
      - "internal error"
