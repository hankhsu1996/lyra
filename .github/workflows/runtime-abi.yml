name: Runtime ABI

on:
  pull_request:

jobs:
  check:
    name: Check runtime ABI policy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Check runtime ABI policy
        run: python3 tools/policy/check_runtime_abi.py
