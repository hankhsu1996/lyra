[benchmark]
name = "edge-sub-dense"
family = "scheduling"
focus = "runtime"
primary = "edge subscription traversal / activation overhead"
secondary = ["per-process dispatch cost"]
regression_means = "scheduling/edge-subscription/activation path regressed"

[tiers]
pr = true
nightly = true

[params.defaults]
NUM_CYCLES = 500000

[params.pr]
NUM_CYCLES = 50000

[params.nightly]
NUM_CYCLES = 500000

[counters]
relevant = []
