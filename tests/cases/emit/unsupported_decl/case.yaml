id: emit.unsupported_decl
tags: [emit, errors]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
expect:
  exit: 1
  stderr:
    contains:
      - "unsupported:"
      - "declarations inside generate scopes are not supported"
