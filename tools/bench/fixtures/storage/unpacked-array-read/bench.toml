[benchmark]
name = "unpacked-array-read"
family = "storage"
focus = "runtime"
primary = "unpacked array read throughput"
secondary = ["loop overhead", "scalar add"]
regression_means = "storage/indexing/load path regressed"

[tiers]
pr = true
nightly = true

[params.defaults]
NUM_ITERS = 4096

[params.pr]
NUM_ITERS = 512

[params.nightly]
NUM_ITERS = 4096

[counters]
relevant = []
