id: cpp.fork_named_block
tags: [cpp_run]
input:
  command: [run, cpp]
  project: false
  top: Test
  files: [main.sv]
expect:
  exit: 0
  stdout:
    exact: |
      [1] worker branch in Test.outer.worker
      [15] labelled fork branch in Test.outer.labelled
      [16] anonymous fork branch in Test.outer
      [18] branch before the disable
      [20] counter = 42
      [20] resumed after the disabled fork
