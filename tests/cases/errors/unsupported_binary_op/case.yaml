id: errors.unsupported_binary_op
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "main.sv:4:"
      - "unsupported:"
      - "compound assignments are not supported yet"
      - "a += 1;"
    not_contains:
      - "internal error"
