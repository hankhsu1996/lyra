id: emit.generate_if
tags: [emit, generate]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
expect:
  exit: 0
  stdout:
    contains:
      - "class Top"
      - "Top() {"
      - "if (1) {"
      - "switch (1) {"
      - "case 0:"
      - "default:"
      - "void process_0()"
      - "x = 1;"
