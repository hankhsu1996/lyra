[benchmark]
name = "nba-heavy"
family = "scheduling"
focus = "runtime"
primary = "NBA queue / commit overhead"
secondary = ["per-edge process activation"]
regression_means = "scheduling/NBA-queue/commit path regressed"

[tiers]
pr = true
nightly = true

[params.defaults]
NUM_CYCLES = 500000

[params.pr]
NUM_CYCLES = 50000

[params.nightly]
NUM_CYCLES = 500000

[counters]
relevant = []
