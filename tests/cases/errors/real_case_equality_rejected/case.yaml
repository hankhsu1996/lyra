id: errors.real_case_equality_rejected
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "error:"
      - "case equality"
      - "real"
    not_contains:
      - "internal error"
