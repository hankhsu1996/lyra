id: cpp.tagged_packed_union_tag_mismatch
tags: [cpp_run]
input:
  command: [run, cpp]
  project: false
  top: Top
  files: [main.sv]
expect:
  exit: 1
  stdout:
    exact: ""
  stderr:
    exact: "runtime error: read of a tagged union member inconsistent with the current tag (LRM 11.9)\n"
