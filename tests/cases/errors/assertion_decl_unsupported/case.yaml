id: errors.assertion_decl_unsupported
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "unsupported:"
      - "checker"
      - "--disable-assertions"
    not_contains:
      - "internal error"
