name: LLVM Backend Boundaries

on:
  pull_request:

jobs:
  check:
    name: Check LLVM backend boundaries
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check LLVM backend boundaries
        run: python3 tools/policy/check_llvm_backend_boundaries.py
