feature: dpi_svdpi_runtime
description: svdpi.h runtime library utility functions (D6a)

cases:
  - name: svdpi_version_available
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_svdpi_version();
        int result;
        initial begin
          result = test_svdpi_version();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: bitsel_bit_roundtrip
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_bitsel_roundtrip();
        int result;
        initial begin
          result = test_bitsel_roundtrip();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: bitsel_logic_roundtrip
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_bitsel_logic_roundtrip();
        int result;
        initial begin
          result = test_bitsel_logic_roundtrip();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: partsel_bit_roundtrip
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_partsel_bit_roundtrip();
        int result;
        initial begin
          result = test_partsel_bit_roundtrip();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: partsel_logic_roundtrip
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_partsel_logic_roundtrip();
        int result;
        initial begin
          result = test_partsel_logic_roundtrip();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: cross_word_boundary
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_cross_word_boundary();
        int result;
        initial begin
          result = test_cross_word_boundary();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: legacy_named_select_entrypoints_work
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_legacy_named_select();
        int result;
        initial begin
          result = test_legacy_named_select();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: legacy_named_bitvec32_entrypoints_work
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_legacy_named_bitvec32();
        int result;
        initial begin
          result = test_legacy_named_bitvec32();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: legacy_named_logicvec32_entrypoints_work
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_legacy_named_logicvec32();
        int result;
        initial begin
          result = test_legacy_named_logicvec32();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: legacy_named_partial_width_entrypoints_work
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_legacy_named_partial_width();
        int result;
        initial begin
          result = test_legacy_named_partial_width();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: legacy_named_partselect_entrypoints_work
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_legacy_named_partselect();
        int result;
        initial begin
          result = test_legacy_named_partselect();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: size_helpers_work
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" pure function int test_size_helpers();
        int result;
        initial begin
          result = test_size_helpers();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_get_returns_null_initially
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_get_returns_null_initially();
        int result;
        initial begin
          result = test_scope_get_returns_null_initially();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_from_name_valid
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_from_name_valid();
        int result;
        initial begin
          result = test_scope_from_name_valid();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_from_name_invalid
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_from_name_invalid();
        int result;
        initial begin
          result = test_scope_from_name_invalid();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_name_roundtrip
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_name_roundtrip();
        int result;
        initial begin
          result = test_scope_name_roundtrip();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_set_get_roundtrip
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_set_get_roundtrip();
        int result;
        initial begin
          result = test_scope_set_get_roundtrip();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_set_returns_previous
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_set_returns_previous();
        int result;
        initial begin
          result = test_scope_set_returns_previous();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_user_data_roundtrip
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_user_data_roundtrip();
        int result;
        initial begin
          result = test_scope_user_data_roundtrip();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_user_data_null_scope
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_user_data_null_scope();
        int result;
        initial begin
          result = test_scope_user_data_null_scope();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: scope_name_from_null
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Test;
        import "DPI-C" function int test_scope_name_from_null();
        int result;
        initial begin
          result = test_scope_name_from_null();
        end
      endmodule
    expect:
      variables:
        result: 1

  - name: generate_scope_registration
    description: >
      Generate-block scopes are first-class in the DPI scope registry.
      svGetScopeFromName resolves "Test.blk[0]", the handle round-trips
      through svGetNameFromScope, svPutUserData/svGetUserData accept the
      generate-scope handle, and the nested instance path is also
      registered. This exercises Cut 5's scope-tree-wide registry, not
      just instance scopes.
    dpi_sources:
      - dpi_helpers/svdpi_utils.c
    sv: |
      module Leaf;
      endmodule

      module Test;
        for (genvar i = 0; i < 2; i++) begin : blk
          Leaf u();
        end
        import "DPI-C" function int test_generate_scope_registration();
        int result;
        initial begin
          result = test_generate_scope_registration();
        end
      endmodule
    expect:
      variables:
        result: 1
