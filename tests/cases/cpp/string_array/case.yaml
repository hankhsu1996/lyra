id: cpp.string_array
tags: [cpp_run]
input:
  command: [run, cpp]
  project: false
  top: Top
  files: [main.sv]
expect:
  exit: 0
  variables:
    sa: ["alpha", "beta", "gamma"]
    sq: ["one", "two", "three"]
    sf: ["x", "y"]
    written: ["alpha", "BETA", "gamma"]
    oob: ""
    eq: 1
