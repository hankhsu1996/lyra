id: errors.dpi_export_unsupported
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "unsupported:"
      - "DPI-C export with an output / inout argument is not yet supported"
    not_contains:
      - "internal error"
      - "\x1b["
