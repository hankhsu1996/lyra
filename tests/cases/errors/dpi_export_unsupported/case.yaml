id: errors.dpi_export_unsupported
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "main.sv:4:"
      - "unsupported:"
      - "DPI-C export of a subroutine is not yet supported"
    not_contains:
      - "internal error"
      - "\x1b["
