[benchmark]
name = "packed-array-write"
category = "process-kernel"
subcategory = "memory"
intent = "Protect packed array element indexing and store path from regression."
focus = "runtime"
primary = "packed array element write throughput"
secondary = ["packed element indexing", "index computation"]

[scale.small]
NUM_ITERS = 50_000

[scale.large]
NUM_ITERS = 500_000
