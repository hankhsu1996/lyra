id: errors.unsupported_type
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "main.sv:2:"
      - "unsupported:"
      - "associative array types are not supported"
      - "int m [string];"
    not_contains:
      - "internal error"
      - "\x1b["
