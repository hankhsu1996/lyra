# Test infrastructure for Lyra
#
# Suite-based test framework using YAML test specs in sv_features/

load("@rules_cc//cc:defs.bzl", "cc_library", "cc_test")

cc_library(
    name = "test_core",
    srcs = glob(
        ["framework/*.cpp"],
        exclude = [
            "framework/runner.cpp",
            "framework/assertions.cpp",
        ],
    ),
    hdrs = glob(
        ["framework/*.hpp"],
        exclude = [
            "framework/runner.hpp",
            "framework/assertions.hpp",
        ],
    ),
    deps = [
        "//:frontend",
        "//:llvm_backend",
        "//:lowering",
        "//:realization",
        "@slang",
        "@yaml-cpp",
    ],
)

cc_library(
    name = "test_assertions",
    srcs = [
        "framework/assertions.cpp",
        "framework/runner.cpp",
    ],
    hdrs = [
        "framework/assertions.hpp",
        "framework/runner.hpp",
    ],
    deps = [
        ":test_core",
        "@googletest//:gtest",
    ],
)

# Test exclusion policy:
# JIT and AOT targets must not diverge through per-target gtest_filter rules.
# If a case is unsupported, express that in YAML expectations or skip metadata,
# not by target-local test filtering.

cc_test(
    name = "jit_dev_tests",
    srcs = ["main.cpp"],
    args = [
        "--suite=jit_dev",
    ],
    data = [
        "suites.yaml",
        "//third_party/systemverilog:svdpi.h",
    ] + glob(["sv_features/**/*.yaml"]) + glob(["sv_features/**/*.c"]),
    shard_count = 4,
    deps = [
        ":test_assertions",
        "@googletest//:gtest",
    ],
)

# JIT two-state suite - regression tests for --two-state mode
cc_test(
    name = "jit_two_state_tests",
    srcs = ["main.cpp"],
    args = ["--suite=jit_two_state"],
    data = [
        "suites.yaml",
        "//third_party/systemverilog:svdpi.h",
    ] + glob(["sv_features/**/*.yaml"]) + glob(["sv_features/**/*.c"]),
    shard_count = 4,
    deps = [
        ":test_assertions",
        "@googletest//:gtest",
    ],
)

# AOT development suite - full AOT pipeline (compile > link > execute)
cc_test(
    name = "aot_dev_tests",
    srcs = ["main.cpp"],
    args = ["--suite=aot_dev"],
    data = [
        "suites.yaml",
        "//:liblyra_runtime.so",
        "//third_party/systemverilog:svdpi.h",
    ] + glob(["sv_features/**/*.yaml"]) + glob(["sv_features/**/*.c"]),
    shard_count = 4,
    deps = [
        ":test_assertions",
        "@googletest//:gtest",
    ],
)

# AOT smoke suite - small, stable subset for fast PR gating
cc_test(
    name = "aot_smoke_tests",
    srcs = ["main.cpp"],
    args = ["--suite=aot_smoke"],
    data = [
        "suites.yaml",
        "//:liblyra_runtime.so",
        "//third_party/systemverilog:svdpi.h",
    ] + glob(["sv_features/**/*.yaml"]) + glob(["sv_features/**/*.c"]),
    deps = [
        ":test_assertions",
        "@googletest//:gtest",
    ],
)

# Unit tests for trace infrastructure (no LLVM/slang dependency)
cc_test(
    name = "trace_manager_test",
    srcs = ["trace_manager_test.cpp"],
    deps = [
        "//:runtime",
        "@googletest//:gtest_main",
    ],
)

cc_test(
    name = "trace_signal_meta_test",
    srcs = ["trace_signal_meta_test.cpp"],
    deps = [
        "//:runtime",
        "@googletest//:gtest_main",
    ],
)

cc_test(
    name = "trace_selection_test",
    srcs = ["trace_selection_test.cpp"],
    deps = [
        "//:runtime",
        "@googletest//:gtest_main",
    ],
)

cc_test(
    name = "canonical_observable_shape_test",
    srcs = ["canonical_observable_shape_test.cpp"],
    deps = [
        "//:llvm_backend",
        "@googletest//:gtest_main",
    ],
)

cc_test(
    name = "resolve_byte_range_test",
    srcs = ["resolve_byte_range_test.cpp"],
    deps = [
        "//:llvm_backend",
        "@googletest//:gtest_main",
    ],
)

cc_test(
    name = "write_plan_test",
    srcs = ["write_plan_test.cpp"],
    deps = [
        "//:llvm_backend",
        "@googletest//:gtest_main",
    ],
)

# LLI development suite - out-of-process LLI execution (for debugging/comparison)
cc_test(
    name = "lli_dev_tests",
    srcs = ["main.cpp"],
    args = ["--suite=lli_dev"],
    data = [
        "suites.yaml",
        "//:liblyra_runtime.so",
        "//third_party/systemverilog:svdpi.h",
    ] + glob(["sv_features/**/*.yaml"]) + glob(["sv_features/**/*.c"]),
    deps = [
        ":test_assertions",
        "@googletest//:gtest",
    ],
)

cc_test(
    name = "dpi_visible_name_collision_test",
    srcs = ["dpi_visible_name_collision_test.cpp"],
    deps = [
        "//:lowering",
        "@googletest//:gtest_main",
    ],
)
