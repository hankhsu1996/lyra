id: emit.local_assign
tags: [emit]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
expect:
  exit: 0
  stdout:
    contains:
      - "auto process_0() -> lyra::runtime::Process"
      - "std::int32_t x{};"
      - "(x = (1 + 2));"
      - "co_return;"
