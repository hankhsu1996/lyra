[benchmark]
name = "packed-array-read"
category = "process-kernel"
subcategory = "memory"
intent = "Protect packed array element indexing and load path from regression."
focus = "runtime"
primary = "packed array element read throughput"
secondary = ["packed element indexing", "loop overhead"]

[scale.small]
NUM_ITERS = 2_560

[scale.large]
NUM_ITERS = 20_480
