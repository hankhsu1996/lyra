id: cpp.fork_named_block
tags: [cpp_run]
input:
  command: [run, cpp]
  project: false
  top: Test
  files: [main.sv]
expect:
  exit: 0
  stdout:
    exact: |
      [15] labelled fork branch
      [20] counter = 42
