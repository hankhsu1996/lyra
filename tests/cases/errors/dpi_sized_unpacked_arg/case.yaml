id: errors.dpi_sized_unpacked_arg
tags: [errors, diag]
input:
  command: [emit, cpp]
  project: false
  top: Top
  files: [main.sv]
  args: ["--no-color"]
expect:
  exit: 1
  stderr:
    contains:
      - "unsupported:"
      - "sized unpacked array"
      - "open array"
    not_contains:
      - "internal error"
